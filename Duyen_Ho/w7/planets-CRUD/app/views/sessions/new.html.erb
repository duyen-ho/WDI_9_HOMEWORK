<h1>Log In</h1>

<%= form_for(:session, url: sessions_path) do |f| %>

  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.label :password  %>
  <%= f.password_field :password %>

  <%= submit_tag("Login") %>

<% end %>
